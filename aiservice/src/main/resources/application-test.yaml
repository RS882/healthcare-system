spring:
  application:
    name: ai-service-test

  config:
    import: ""

  cloud:
    config:
      enabled: false
      fail-fast: false
    discovery:
      enabled: false

  cache:
    type: redis

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3:latest

cache:
  default-ttl: 5m
  active-prompt:
    ttl: 10m
  user-auth-info:
    ttl: 10s

ai:
  provider: ollama
  model: llama3:latest

  prompt:
    activation-retry:
      max-attempts: 3
      initial-delay: 1ms
      multiplier: 1.1
      max-delay: 2ms

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false